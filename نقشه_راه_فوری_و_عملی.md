# 🚀 نقشه راه فوری و عملی برای پروژه Trading AI System

## 📋 لیست اقدامات فوری (اولویت‌بندی شده)

### **🔴 فاز 1: رفع فوری مشکلات بحرانی (3-4 روز)**

#### **روز 1: Thread Safety**
```python
# در trading_ai_system/core/core.py تغییرات:
from threading import Lock, RLock

class GlobalState:
    _instance = None
    _lock = Lock()
    _state = {}
    
    @classmethod
    def get_instance(cls):
        with cls._lock:
            if cls._instance is None:
                cls._instance = cls()
            return cls._instance

# در trading_ai_system/live/live.py:
class LiveTradingEngine:
    def __init__(self):
        self._order_lock = RLock()
        self._position_lock = RLock()
```

**تست فوری:**
```python
python -c "from trading_ai_system.core.core import GlobalState; print('Thread-safe GlobalState ready')"
```

#### **روز 2: Broker Reconnection**
```python
# در trading_ai_system/live/live.py اضافه کنید:
import time
from functools import wraps

def retry_with_backoff(max_retries=3, backoff_factor=2, base_wait=1):
    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            for attempt in range(max_retries):
                try:
                    return func(*args, **kwargs)
                except Exception as e:
                    if attempt == max_retries - 1:
                        raise
                    wait_time = base_wait * (backoff_factor ** attempt)
                    time.sleep(wait_time)
            return wrapper
    return decorator

class BrokerConnector:
    @retry_with_backoff(max_retries=3)
    def connect(self):
        # connection logic here
        pass
```

**تست فوری:**
```bash
python -m pytest tests/test_live.py -xvs -k "test_reconnection"
```

#### **روز 3: Error Handling پایه**
```python
# در trading_ai_system/core/core.py:
class ErrorRecovery:
    @staticmethod
    def safe_execute(func, *args, fallback=None, max_retries=2, **kwargs):
        for attempt in range(max_retries + 1):
            try:
                return func(*args, **kwargs)
            except Exception as e:
                if attempt == max_retries:
                    if fallback is not None:
                        return fallback
                    raise
                logger.warning(f"Attempt {attempt + 1} failed: {e}")
```

### **🟠 فاز 2: بهبود سریع عملکرد (2-3 روز)**

#### **روز 4: Caching ساده**
```python
# در trading_ai_system/data/data.py:
import functools
from datetime import datetime, timedelta

def cache_with_ttl(ttl_seconds=300):
    def decorator(func):
        cache = {}
        @functools.wraps(func)
        def wrapper(*args, **kwargs):
            key = str(args) + str(kwargs)
            now = datetime.now()
            if key in cache:
                value, timestamp = cache[key]
                if now - timestamp < timedelta(seconds=ttl_seconds):
                    return value
            
            result = func(*args, **kwargs)
            cache[key] = (result, now)
            return result
        return wrapper
    return decorator

class DataFetcher:
    @cache_with_ttl(ttl_seconds=60)
    def fetch_ohlcv(self, symbol, timeframe):
        # fetch logic
        pass
```

#### **روز 5: Performance Profiling**
```python
# ایجاد فایل performance_profiler.py در ریشه پروژه:
import cProfile
import pstats
from trading_ai_system import main

def profile_trading_cycle():
    profiler = cProfile.Profile()
    profiler.enable()
    
    # یک cycle کامل از main را اجرا کنید
    # (نیاز به تنظیم دارد)
    
    profiler.disable()
    stats = pstats.Stats(profiler).sort_stats('cumulative')
    stats.print_stats(20)
    
    # Save to file
    stats.dump_stats('performance_profile.prof')
```

**اجرای فوری:**
```bash
python performance_profiler.py
```

### **🟢 فاز 3: افزایش Test Coverage (4-5 روز)**

#### **روز 6-7: Core Tests**
```python
# ایجاد tests/test_core_improved.py:
import pytest
from threading import Thread
from trading_ai_system.core.core import GlobalState

def test_global_state_thread_safety():
    """Test that GlobalState is thread-safe."""
    results = []
    
    def worker():
        instance = GlobalState.get_instance()
        results.append(instance)
    
    threads = [Thread(target=worker) for _ in range(10)]
    for t in threads:
        t.start()
    for t in threads:
        t.join()
    
    # همه threads باید instance یکسانی بگیرند
    assert len(set(results)) == 1

def test_error_recovery():
    """Test error recovery mechanism."""
    from trading_ai_system.core.core import ErrorRecovery
    
    def failing_func():
        raise ValueError("Test error")
    
    fallback_value = "fallback"
    result = ErrorRecovery.safe_execute(
        failing_func, 
        fallback=fallback_value,
        max_retries=2
    )
    assert result == fallback_value
```

#### **روز 8-9: Live Module Tests**
```python
# ایجاد tests/test_live_critical.py:
import pytest
from unittest.mock import Mock, patch
from trading_ai_system.live.live import BrokerConnector

def test_broker_reconnection():
    """Test that broker reconnects after failure."""
    broker = BrokerConnector()
    
    # Mock connection to fail first, then succeed
    call_count = 0
    def mock_connect():
        nonlocal call_count
        call_count += 1
        if call_count == 1:
            raise ConnectionError("First attempt fails")
        return "connected"
    
    broker.connect = mock_connect
    
    # Should succeed on retry
    result = broker.connect()
    assert result == "connected"
    assert call_count == 2
```

#### **روز 10: Integration Tests**
```python
# ایجاد tests/test_integration_basic.py:
import pandas as pd
import numpy as np
from trading_ai_system.data.data import DataFetcher
from trading_ai_system.features.features import FeatureEngineer
from trading_ai_system.models.models import LGBModel

def test_full_pipeline():
    """Test the complete trading pipeline."""
    # 1. Create sample data
    dates = pd.date_range('2023-01-01', periods=100, freq='D')
    data = pd.DataFrame({
        'open': np.random.randn(100).cumsum() + 100,
        'high': np.random.randn(100).cumsum() + 102,
        'low': np.random.randn(100).cumsum() + 98,
        'close': np.random.randn(100).cumsum() + 101,
        'volume': np.random.randint(1000, 10000, 100)
    }, index=dates)
    
    # 2. Calculate features
    engineer = FeatureEngineer()
    features = engineer.calculate(data)
    assert not features.isnull().any().any()
    
    # 3. Create dummy labels (for testing)
    labels = (data['close'].shift(-1) > data['close']).astype(int)
    labels = labels[:-1]  # Remove last NaN
    
    # 4. Train simple model
    model = LGBModel(name='test_model')
    model.train(features.iloc[:-1], labels)
    
    # 5. Make prediction
    prediction = model.predict(features.iloc[-1:])
    assert prediction is not None
    
    print("✓ Full pipeline works!")
```

### **📊 اجرای تست‌ها**
```bash
# روزانه این تست‌ها را اجرا کنید:
cd trading_ai_system_refactored

# روز 1-3: Thread Safety و Reconnection
pytest tests/test_core_improved.py -v

# روز 4-5: Performance
python performance_profiler.py

# روز 6-10: همه تست‌ها
pytest tests/ -v --cov=trading_ai_system --cov-report=html

# بررسی coverage:
open htmlcov/index.html  # روی مک
# یا
start htmlcov/index.html  # روی ویندوز
```

### **🎯 اهداف هفتگی**

#### **هفته 1: Stability**
- [ ] Thread safety کامل
- [ ] Broker reconnection با retry
- [ ] Basic error recovery
- [ ] 10+ تست جدید

#### **هفته 2: Performance**
- [ ] Caching برای data fetching
- [ ] Performance profiling انجام شود
- [ ] 20% بهبود در latency
- [ ] 20+ تست جدید

#### **هفته 3: Quality**
- [ ] Test coverage به 50% برسد
- [ ] Type hints برای core module کامل
- [ ] Documentation برای critical paths
- [ ] 30+ تست جدید

#### **هفته 4: Production Readiness**
- [ ] تمام critical bugs رفع شده
- [ ] Test coverage 60%+
- [ ] Performance قابل قبول (<500ms)
- [ ] Monitoring basic setup

### **⚡ Quick Wins (کارهای سریع با impact بالا)**

#### **1. Logging Improvement (30 دقیقه)**
```python
# در trading_ai_system/core/core.py:
import logging
import json

class StructuredLogger:
    @staticmethod
    def log_trade(signal, confidence, price, volume):
        log_data = {
            "timestamp": datetime.now().isoformat(),
            "signal": signal,
            "confidence": confidence,
            "price": price,
            "volume": volume,
            "type": "trade"
        }
        logger.info(json.dumps(log_data))
```

#### **2. Configuration Management (1 ساعت)**
```python
# ایجاد configs/trading_config.yaml:
trading:
  pair: "EURUSD"
  timeframe: "1h"
  max_position: 0.05
  stop_loss: 0.02
  take_profit: 0.04

risk:
  max_drawdown: 0.15
  max_daily_loss: 0.05
  position_concentration: 0.20

# لود کردن در core.py:
import yaml

def load_config(config_path="configs/trading_config.yaml"):
    with open(config_path) as f:
        return yaml.safe_load(f)
```

#### **3. Health Check Endpoint (45 دقیقه)**
```python
# ایجاد monitoring/health.py:
from flask import Flask, jsonify
import psutil

app = Flask(__name__)

@app.route('/health')
def health_check():
    return jsonify({
        "status": "healthy",
        "memory_usage": psutil.Process().memory_percent(),
        "cpu_usage": psutil.cpu_percent(interval=1),
        "uptime": "todo"  # Add uptime tracking
    })

# اجرا در پس‌زمینه:
# python -m monitoring.health
```

### **📈 معیارهای پیشرفت روزانه**

| روز | هدف | معیار موفقیت |
|-----|------|--------------|
| 1 | Thread Safety | 0 race conditions در تست‌ها |
| 2 | Broker Reconnection | 3 بار retry موفق |
| 3 | Error Recovery | 90% خطاها recover شوند |
| 4 | Caching | 50% کاهش data fetch time |
| 5 | Performance Profiling | شناسایی 3 bottleneck اصلی |
| 6-7 | Core Tests | 80% coverage برای core |
| 8-9 | Live Tests | 70% coverage برای live |
| 10 | Integration Tests | Pipeline کامل بدون error |

### **🔧 Tools Setup (اولین روز)**

```bash
# 1. Environment Setup
python -m venv venv
source venv/bin/activate  # یا venv\Scripts\activate روی ویندوز
pip install -e ".[dev]"

# 2. Code Quality Tools
pre-commit install
black --check trading_ai_system/
flake8 trading_ai_system/
mypy trading_ai_system/

# 3. Test Setup
pytest --version
pytest tests/ --collect-only | wc -l  # تعداد تست‌های فعلی

# 4. Performance Tools
pip install line_profiler memory_profiler

# 5. Monitoring Tools
pip install psutil flask
```

### **🎯 نتیجه نهایی بعد از 4 هفته**

بعد از 4 هفته، سیستم شما باید:

1. **Stable**: بدون crash بخاطر thread issues
2. **Reliable**: Broker disconnections رفع شده
3. **Tested**: 60%+ test coverage
4. **Performant**: <500ms per trade cycle
5. **Monitorable**: Health checks و basic metrics
6. **Production Ready**: برای deployment آماده

**گام بعدی**: بعد از این 4 هفته، بر روی advanced features و scaling تمرکز کنید.

---

**تاریخ ایجاد**: 15 بهمن 1403  
**آخرین بروزرسانی**: 15 بهمن 1403  
**وضعیت**: Ready for Immediate Execution  
**مدت زمان تخمینی**: 4 هفته (80-100 ساعت)  

📞 شروع کنید با روز 1: Thread Safety Fixes!
